{
    "private": true,
    "scripts": {
        "dev": "cd packages/ads/front && yarn install && yarn dev",
        "build": "cd packages/ads/front && yarn install && yarn build",
        "lint": "cd packages/ads/front && yarn install && yarn lint && yarn ts-lint"
    }
}
